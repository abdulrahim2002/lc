/**
 * @param {number} x
 * @return {number}
 */
var reverse = function(x) {
    /**
     * we are only allowed to work within 32 bits, which implies we
     * cannot do arithematic here. Since comparing the number will
     * also take up space and it will overflow
     */
    sign = x>=0 ? true : false;
    let s = "";
    while (x){

    }
    return x;
};
